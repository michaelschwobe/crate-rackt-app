// The order of imports controls the order shown within the addon panel.
// It can also be overridden via `selectedAddonPanel` in .storybook/config.js.
import '@storybook/addon-knobs/register';
import '@storybook/addon-actions/register';
import '@storybook/addon-links/register';
